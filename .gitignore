# ---- Dependencies ----
node_modules/

# ---- Build output ----
dist/
build/
.next/
out/
*.tsbuildinfo
next-env.d.ts.bak

# ---- Test / coverage output ----
coverage/
/test-results/
/playwright-report/
/blob-report/

# ---- Environment ----
# .env.example is committed; real credentials never are.
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# ---- Runtime artefacts ----
# Uploaded PDFs live on a volume (Docker) or here (host mode); neither belongs in git.
uploads/
*.log
npm-debug.log*
yarn-error.log*

# ---- Deployment tooling ----
.vercel/
.turbo/

# ---- Editor / OS ----
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# ---- Not part of the submission ----
# The assignment brief itself — the repo is the answer, not a copy of the question.
SuretySeven_SDE1_Take_Home_Assignment.pdf

# The build log — working context for this repo, not part of the submission.
CLAUDE.md
